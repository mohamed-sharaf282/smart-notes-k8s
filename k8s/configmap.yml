apiVersion: v1
kind: ConfigMap

metadata:
  name: backend-config
  namespace: smart-notes

data:
  APP_ENV: production
  APP_VERSION: v3

  DB_HOST: postgres-service
  DB_NAME: postgres
  DB_USER: postgres